#PROD environment

app:
  profile: PROD

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/<MYSQL_URL>
    username: <USERNAME>
    password: <PASSWORD>

    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: update
